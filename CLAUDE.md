# Couple Lab — מצב נוכחי והמשך עבודה

מסמך זה נקרא אוטומטית בתחילת סשן. הוא מתאר איפה הפרויקט עומד **היום**
(14.8.2026) ומה פתוח. לארכיטקטורה המלאה ראו `docs/ARCHITECTURE.md`,
ולכללי העבודה `AGENTS.md`.

## שלוש עובדות שחוסכות סיבוב שלם

1. **האפליקציה רצה מ־`dist/`, לא מ־`src/`.** הקיצור בשולחן העבודה מפעיל
   Electron שטוען את החבילה הבנויה. עריכת `src/` בלי `npm run build`
   **לא משנה כלום** במה שהמשתמש רואה. זה כבר עלה סיבוב שלם של בדיקות שווא.
   `electron/*.cjs` לעומת זאת נקראים מהדיסק בכל הפעלה.
2. **הפרויקט הפעיל הוא זה.** `C:\Users\User\couple-lab` הוא פיצול נטוש
   מ־7.7.2026 — להתעלם.
3. **בדיקה אמיתית = More ← אבחון ← ייצוא.** ה־JSON הזה הוא מקור האמת
   היחיד לשאלה "האם זה עבד". אין דרך אחרת לדעת.

## פקודות

```bash
npm run build          # tsc --noEmit + vite build  (חובה אחרי כל שינוי ב-src/)
npm test               # 88 בדיקות Vitest
npm run desktop:smoke  # Electron smoke — טרם הורץ אחרי שינויי 14.8
npm run desktop:dist   # portable exe
```

## מה נעשה ב־14.8.2026 (סשן Cowork)

כל השינויים **לא נכנסו ל־git**. המשימה הראשונה: לעבור עליהם ולעשות commit.

### תיקוני תשתית תמלול — הגדולים

| מה | איפה | למה |
|---|---|---|
| `detector.front(false)` | `electron/transcription-worker.cjs` | ברירת המחדל `true` מחזירה ArrayBuffer חיצוני; Electron זורק `External buffers are not allowed`, ה־VAD קרס בשקט וכל תמלול ירד ל־`full-audio-fallback`. |
| מיזוג חלונות פענוח לפי פער (1.2ש׳) במקום חפיפה | `electron/transcription-pipeline.cjs` | תנאי המיזוג דרש חפיפה ממש; הריפוד גישר 0.4ש׳ ו־Silero מפצל על 0.55ש׳ — כלומר **שום דבר לא התמזג אי פעם**. כל אמירה פוענחה לבד: Whisper בלי הקשר (ממציא מילים) ומעבר קודד מלא לכל קטע (איטי פי 4). |
| שגיאת fallback נשמרת ומדווחת | worker + `App.tsx` | ה־catch בלע את הסיבה. בלי זה אי אפשר היה לאבחן. |
| סניטציה במקום פסילה | `validateTranscriptionPayload` | דגימה אחת מחוץ לטווח זרקה שיחה שלמה. |
| `AudioContext({sampleRate:16000})` | `App.tsx`, אשף הכיול | resampling מסונן של הדפדפן במקום אינטרפולציה לינארית עם aliasing; גם פחות CPU. |

### יכולות חדשות

- **כיול תמלול אוטומטי** (`src/transcriptionCalibration.ts`) — משפטי הקריאה
  באשף ידועים מראש, אז אותה הקלטה מתומללת ו־WER נמדד לבד. אין הזנה ידנית.
  המשפטים נבנים מהשמות האמיתיים ומהתחום החזק/החלש במפת הקשר.
- **מתקין מודל בתוך האפליקציה** — הגדרות ← מנוע התמלול. מוריד חבילת
  sherpa-onnx רשמית דרך `net` של Electron עם פס התקדמות, מחליף worker בלי
  restart. ראו `docs/TRANSCRIPTION_UPGRADE.md`.
- **שיוך דובר אוטומטי** — CAM++ על מקטעי VAD ≥1.2ש׳. נחסם במפורש כשאין
  גבולות VAD אמיתיים, אחרת כל השיחה מתויגת לאדם אחד.
- **רגעי זהב** (`src/goldenMoments.ts`) — עד 3 קטעים קצרים מההקלטה סביב
  רגעים חיוביים. seek בלבד, בלי קידוד מחדש.
- **חיפוש בשיחות** (`src/transcriptSearch.ts`) — נרמול niqqud ואותיות
  סופיות, הדגשה ממופה לטקסט המקורי.
- **טלמטריה חזותית** — `vision.session_summary` בכל שיחה.

### תיקונים קטנים

`CONTEMPT_RISK_LABEL` במקום מחרוזת מועתקת · `try/catch` על localStorage ·
איחוד רשימות Ollama · מפתחות מחיקה חסרים · הסרת אחוזי confidence מהממשק ·
`profileRef` כדי שלולאת הדגימה לא תיבנה מחדש בכל כיול · 720p + VP9 +
בדיקת תאורה.

## מה פתוח

**דחוף**
- `git add/commit` — אין היסטוריה לשום דבר מלמעלה.
- `npm run desktop:smoke` — לא הורץ.
- לאמת שהמשתמש התקין את `whisper-turbo` ולמדוד WER מחדש בכיול.

**החלטות מוצר שממתינות למשתמש**
- **תמלול/ניתוח בענן.** ישפר דרמטית, אבל סותר הבטחה מפורשת בממשק
  ("ההקלטות אינן נשלחות לענן") וב־`PRODUCT.md`. דורש שכתוב הסכמה + CSP.
  אמצע אפשרי: תמלול מקומי, שליחת **טקסט בלבד** לניתוח.
- **זיהוי רגשות.** ההמלצה: **לא** קטגוריות (AffectNet 8-class מגיע ל־63%
  במקרה הטוב — whisper-tiny מחדש), אלא **valence-arousal** כמגמה מוחלקת
  בלבד, מ־`enet_b0_8_va_mtl` (16MB, Apache-2.0), ורק כשהתמלול מאשר.

**חוב טכני מוכר** — ראו `COUPLE_LAB_REVIEW.md`
- `src/App.tsx` ~5,100 שורות; `PracticeStudio` ~1,800 עם 48 refs. תוכנית
  פירוק בארבעה שלבים במסמך.
- סבב קופי: להסיר "אפשרי" מ־13 תוויות, לאחד טרמינולוגיה, לתקן מגדר,
  להשאיר הסתייגות אחת במקום שש. דורש `i18n/labels.he.ts`.
- `public/adam-porat-*` — ~15MB לא בשימוש בכל build.
  `scripts/remove-unused-assets.ps1` מוכן, טרם הורץ.
- קבועי כוונון (ספי מצלמה ואודיו) מפוזרים כמספרי קסם.

## גבולות מוצר שאין לחצות

מ־`PRODUCT.md` ומ־`docs/GOTTMAN_INFORMED_QA.md`:

- אין להציג ניתוח כאבחון, או כקביעה על רגש, כוונה, אשמה או עתיד הקשר.
- שיוך דובר הוא הסתברותי. כשאין ודאות — לומר "לא זוהה", לא לנחש.
- רמזי מצלמה הם גאומטריה. הם לא מעלים flooding, לא יוצרים tags של מצב
  רגשי, ולא מספיקים כראיה בפני עצמם.
- הכול מקומי. אין ענן בלי החלטה מפורשת של המשתמש.
- הממשק בעברית RTL בלבד עד תרגום מלא.
