# הנחיות למפתחים ולסוכני AI

לפני שינוי בקוד, יש לקרוא את [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md). זהו מסמך המקור לארכיטקטורה הנוכחית של Couple Lab.

בעבודה על וידאו בזמן אמת, מודלים, גיימיפיקציה או Android יש לקרוא גם את [`docs/REALTIME_ANDROID_ROADMAP.md`](docs/REALTIME_ANDROID_ROADMAP.md). יש להבחין בין Stage שכבר יושם לבין שלב מחקרי עתידי.

## כלל חובה: הארכיטקטורה מתעדכנת יחד עם הקוד

בכל שינוי שמשפיע על מבנה המערכת, זרימת מידע, מודל נתונים, אחסון, אינטגרציה, פרטיות, בטיחות, תהליך build/run או אחריות של קובץ/מודול:

1. לעדכן את `docs/ARCHITECTURE.md` באותה משימת פיתוח.
2. לתקן תיאורים קיימים שהתיישנו; לא רק להוסיף הערה חדשה.
3. לעדכן את השדה "עודכן לאחרונה" ואת יומן השינויים שבסוף המסמך.
4. אם לא נדרש עדכון ארכיטקטורה, לציין בסיכום העבודה: "Architecture impact: none".
5. לא לסמן עבודה כהושלמה לפני הרצת `npm run build` ותיעוד תוצאת הבדיקה.

שינויים חזותיים או שינויי טקסט בלבד אינם מחייבים שינוי במסמך, אלא אם הם משנים זרימה, התנהגות, הרשאות, שמירת מידע או הבטחה למשתמש.

## גבולות הפרויקט

- האפליקציה הראשית נמצאת ב־`src/`, עם מעטפת פיתוח ב־`electron/`.
- `adam-porat-graduation-animation/` הוא פרויקט HyperFrames עצמאי ליצירת נכס מדיה. יש לפעול גם לפי `AGENTS.md` שבתוכו כשעובדים עליו.
- `public/` מכיל נכסים סטטיים; קובצי MP4/WebP אינם חלק מלוגיקת האפליקציה.
- אין להציג את מנוע הניתוח כאבחון קליני או כמקור ודאות לגבי רגשות, כוונות, אלימות או עתיד הקשר.
