# Packaged MediaPipe Tasks Vision runtime

The files in `wasm/` were copied from `@mediapipe/tasks-vision` version 0.10.35 in this project's installed dependencies. They are packaged so the Windows application does not need a CDN connection during calibration or visual analysis.

